all:
  children:
    nocix:
      hosts:
        nocix_server_1 ansible_host=""
      vars:
        ansible_host: "{{ lookup('file', 'secrets/nocix_server_1') }}" # Path is relative from where playbook is run